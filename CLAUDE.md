# Claude Code Instructions

Read `AGENTS.md` first. This repository is a specification and tooling project, not a React Native boilerplate.

Before implementation:

- Identify the spec, rule, template, or package being changed.
- Keep CLI behavior deterministic.
- Run `pnpm lint` and `pnpm test` when possible.
