# Copilot Instructions

This repository follows react-native-vibe-spec v0.1.

- Work from spec, plan, tasks, implementation, checks, review.
- Keep package boundaries intact: CLI delegates to core.
- Do not add runtime dependencies without a clear reason.
- Update templates, rules, examples, and checks together when changing the spec surface.
- Do not commit secrets or example values that look like real credentials.
