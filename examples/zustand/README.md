# Example: Zustand

Zustand feature specs should include:

- Store ownership and action-only mutation paths
- Middleware use, especially persist or devtools
- Selector strategy for component subscriptions
- Offline queue or sync behavior when relevant
- Persistence scope and expiry rules
- Sensitive fields that must never enter the store
