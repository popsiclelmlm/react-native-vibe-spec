# Example: Auth Flow

This example describes the expected files for an auth feature:

```text
features/auth-login/
  spec.md
  plan.md
  tasks.md
  acceptance.md
```

Use:

```bash
rnvibe new feature auth-login
```

The generated spec should be filled with platform behavior, token storage rules, navigation redirects, API contracts, and tests before implementation starts.
