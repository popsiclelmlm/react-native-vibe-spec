# Example: Offline-First List

An offline-first list feature should specify:

- Local cache ownership
- Sync trigger
- Conflict behavior
- Retry behavior
- Empty, loading, offline, and error states
- E2E coverage for offline create and later sync

This is intentionally a workflow example, not a full app boilerplate.
