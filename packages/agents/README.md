# @react-native-vibe-spec/agents

Agent instruction pack for React Native AI coding workflows.

The first version is generated through:

```bash
rnvibe generate agents --agent codex,copilot,claude,cursor
```

Planned assets:

- Codex skill
- Claude Code instructions
- GitHub Copilot repository instructions
- Cursor rules
- Review prompt fragments tied to the versioned spec
